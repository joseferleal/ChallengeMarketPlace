const nuevoProducto=(Name, PicUrl)=>{
    const card=document.createElement("div")
    const contenido=hola/* `Pegar todo el html de la galería aquí y reemplazar img src con ${}` */
    card.innerHTML=contenido;
    card.dataset.id=id;

    return card 
};


const productos=document.querySelector("[data-product]")